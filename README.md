# React Redux - Server-side Rendering (SSR)

**Create the .env file**

```sh
cp .env.example .env
```

**Install dependencies**

`npm install`

**Mode of development**

`npm start`

**Production**

`npm run build`
